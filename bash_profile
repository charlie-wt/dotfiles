# === Init =============================================================================
[ -r "$HOME/.bashrc" ] && . "$HOME/.bashrc"


# === Other ============================================================================
# enable core dumps
ulimit -c unlimited


# === End ==============================================================================
[ -r "$DOTFILES/local/bash_profile" ] && . "$DOTFILES/local/bash_profile"